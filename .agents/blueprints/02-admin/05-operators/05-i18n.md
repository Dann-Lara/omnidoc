# Blueprint: Fase 5 - i18n (OPERATOR)

**Objetivo**: Agregar traducciones necesarias para el módulo de operators.

---

## Archivo: `apps/web/src/lib/i18n/translations.ts`

Agregar nuevas claves en la sección `admin`:

```typescript
export const translations = {
  // ...secciones existentes...
  admin: {
    // ...claves existentes...
    operators: {
      description: { en: 'Manage platform operators and their tenant assignments', es: 'Gestiona los operadores de la plataforma y sus asignaciones de tenants' },
      addNew: { en: 'Add Operator', es: 'Agregar Operador' },
      inviteTitle: { en: 'Invite Operator', es: 'Invitar Operador' },
      selectTenants: { en: 'Select Tenants', es: 'Seleccionar Tenants' },
      selectTenantsPlaceholder: { en: 'Choose tenants...', es: 'Elegir tenants...' },
      tenantsAssigned: { en: 'Tenants Assigned', es: 'Tenants Asignados' },
      sendInvite: { en: 'Send Invitation', es: 'Enviar Invitación' },
      noTenants: { en: 'No tenants assigned', es: 'Sin tenants asignados' },
      invitationSent: { en: 'Invitation sent successfully', es: 'Invitación enviada exitosamente' },
      editTenants: { en: 'Edit Tenant Assignments', es: 'Editar Asignaciones de Tenants' },
      status: { en: 'Status', es: 'Estado' },
      active: { en: 'Active', es: 'Activo' },
      inactive: { en: 'Inactive', es: 'Inactivo' },
      actions: { en: 'Actions', es: 'Acciones' },
      deactivate: { en: 'Deactivate', es: 'Desactivar' },
      reactivate: { en: 'Reactivate', es: 'Reactivar' },
      confirmDeactivate: { en: 'Are you sure you want to deactivate this operator?', es: '¿Estás seguro de que deseas desactivar este operador?' },
    },
  },
}
```

---

## Usage en Componentes

### Listado Operators
```tsx
const { t } = useI18n()

// En el template
{t('admin.operators.description')}
{t('admin.operators.addNew')}
{t('admin.operators.tenantsAssigned')}
{t('admin.operators.status')}
{t('admin.operators.actions')}
```

### Formulario Invitación
```tsx
{t('admin.operators.inviteTitle')}
{t('admin.operators.selectTenants')}
{t('admin.operators.selectTenantsPlaceholder')}
{t('admin.operators.sendInvite')}
```

### Tabla
```tsx
{t('admin.operators.active')}
{t('admin.operators.inactive')}
{t('admin.operators.deactivate')}
{t('admin.operators.editTenants')}
```

---

## Validación

- [ ] Todas las claves en EN y ES
- [ ] `useI18n()` funcionando en las páginas
- [ ] No hardcodear texto inline

---

## Notes

-Mantener el patrón `area.feature.label` para claves
- Traducciones por defecto en ES (por convención del proyecto)
- Verificar que no haya conflictos con claves existentes